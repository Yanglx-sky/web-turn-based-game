<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.gamemodules.mapper.UserAchievementProgressMapper">

    <select id="selectByUserId" resultType="cn.iocoder.gamemodules.entity.UserAchievementProgress">
        SELECT * FROM user_achievement_progress
        WHERE user_id = #{userId}
    </select>

    <select id="selectByUserIdAndAchievementId" resultType="cn.iocoder.gamemodules.entity.UserAchievementProgress">
        SELECT * FROM user_achievement_progress
        WHERE user_id = #{userId} AND achievement_id = #{achievementId}
    </select>

</mapper>